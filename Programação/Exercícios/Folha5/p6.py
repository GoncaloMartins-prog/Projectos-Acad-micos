def aumenta(num):
    """Acrescenta um zero a num

    Requires: num seja um numero inteiro positivo
    Ensures: devolver um numero inteiro com mais
            um zero ah direita de num
    """
    return num*10
#n = int(input("Numero inteiro >0: "))
#print(aumenta(n))
